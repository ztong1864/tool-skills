[
  {
    "name": "RNAseq_edger_limma_de",
    "type": "EdgeRLimmaTool",
    "description": "Bulk RNA-seq differential expression via edgeR (QL-F) or limma-voom on a count matrix + sample metadata — the two standard DE frameworks alongside DESeq2 (run_deseq2_analysis). method='edger' (good for small replicate counts) or 'limma' (large n / complex designs). Returns DEG counts at FDR-only, FDR+|logFC|, and strict (FDR+|logFC|+expression) filters, optional per-gene logFC/FDR, and the full ranked table on disk. Requires Rscript with Bioconductor edgeR + limma.",
    "requires_local_input": true,
    "parameter": {
      "type": "object",
      "required": ["counts_file", "metadata_file", "contrast", "method"],
      "properties": {
        "counts_file": {"type": "string", "description": "Path to counts CSV (genes x samples; first column = gene IDs)"},
        "metadata_file": {"type": "string", "description": "Path to sample metadata CSV (one row per sample)"},
        "design": {"type": ["string", "null"], "description": "Design formula, e.g. '~ condition' or '~ batch + condition' (default '~ condition')"},
        "contrast": {"type": "string", "description": "Contrast as 'factor,level1,level2' (tests level1 vs level2)"},
        "method": {"type": "string", "enum": ["edger", "limma"], "description": "DE framework: 'edger' (QL-F) or 'limma' (limma-voom)"},
        "lfc_threshold": {"type": ["number", "null"], "description": "abs(logFC) threshold for the sig sets (default 0.5)"},
        "fdr_threshold": {"type": ["number", "null"], "description": "FDR (adjusted p) threshold (default 0.05)"},
        "expr_threshold": {"type": ["number", "null"], "description": "Expression threshold (logCPM/AveExpr) for the strict set (default 0)"},
        "exclude_samples": {"type": ["string", "null"], "description": "Comma-separated sample IDs to drop before analysis"},
        "report_genes": {"type": ["string", "null"], "description": "Comma-separated gene IDs to report individual logFC/FDR for"}
      }
    },
    "return_schema": {
      "oneOf": [
        {"type": "object"},
        {"type": "object", "properties": {"error": {"type": "string"}}, "required": ["error"]}
      ]
    },
    "test_examples": [
      {"counts_file": "/tmp/counts.csv", "metadata_file": "/tmp/meta.csv", "design": "~ condition", "contrast": "condition,treated,control", "method": "edger"}
    ]
  }
]
