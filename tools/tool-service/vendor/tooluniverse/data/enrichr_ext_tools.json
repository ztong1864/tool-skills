[
  {
    "name": "Enrichr_list_libraries",
    "description": "List all available Enrichr gene set libraries with statistics. Enrichr has 225+ libraries covering pathways (KEGG, Reactome, WikiPathways), gene ontology (GO), transcription factors, diseases, drugs, and more. Optionally filter by keyword in library name. Set species to query the fly, worm, yeast, or fish Enrichr instances; libraries and gene symbols differ per organism.",
    "type": "EnrichrExtTool",
    "parameter": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "const": "list_libraries",
          "description": "Operation type (fixed: list_libraries)",
          "default": "list_libraries"
        },
        "category": {
          "type": "string",
          "description": "Optional keyword to filter libraries (e.g., 'GO', 'KEGG', 'Reactome', 'disease', 'drug')"
        },
        "species": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            null,
            "human",
            "mouse",
            "fly",
            "worm",
            "yeast",
            "fish"
          ],
          "description": "Organism instance to query: 'human' (default), 'mouse', 'fly' (FlyEnrichr), 'worm' (WormEnrichr), 'yeast' (YeastEnrichr), or 'fish' (FishEnrichr). Gene set libraries differ per organism, and gene symbols must match that organism's nomenclature."
        }
      },
      "required": []
    },
    "return_schema": {
      "type": "array"
    },
    "test_examples": [
      {
        "category": "KEGG"
      },
      {
        "category": "GO"
      }
    ],
    "fields": {
      "operation": "list_libraries"
    }
  },
  {
    "name": "Enrichr_enrich",
    "description": "Perform gene set enrichment analysis using Enrichr. Submit a list of genes and get enriched terms from a specific library. Returns ranked terms with p-values, z-scores, combined scores, and overlapping genes. No authentication required. Set species to query the fly, worm, yeast, or fish Enrichr instances; libraries and gene symbols differ per organism.",
    "type": "EnrichrExtTool",
    "parameter": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "const": "enrich",
          "description": "Operation type (fixed: enrich)",
          "default": "enrich"
        },
        "gene_list": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of gene symbols (e.g., ['TP53', 'BRCA1', 'EGFR']). Minimum 1 gene."
        },
        "library": {
          "type": "string",
          "description": "Enrichr library name (default: GO_Biological_Process_2023). Common: GO_Biological_Process_2023, KEGG_2021_Human, Reactome_2022, WikiPathways_2024_Human, MSigDB_Hallmark_2020",
          "default": "GO_Biological_Process_2023"
        },
        "top_n": {
          "type": "integer",
          "description": "Number of top enriched terms to return (default: 10)",
          "default": 10
        },
        "species": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            null,
            "human",
            "mouse",
            "fly",
            "worm",
            "yeast",
            "fish"
          ],
          "description": "Organism instance to query: 'human' (default), 'mouse', 'fly' (FlyEnrichr), 'worm' (WormEnrichr), 'yeast' (YeastEnrichr), or 'fish' (FishEnrichr). Gene set libraries differ per organism, and gene symbols must match that organism's nomenclature."
        }
      },
      "required": [
        "gene_list"
      ]
    },
    "return_schema": {
      "type": "object"
    },
    "test_examples": [
      {
        "gene_list": [
          "TP53",
          "BRCA1",
          "EGFR",
          "KRAS",
          "MYC",
          "BRAF",
          "PTEN"
        ],
        "library": "GO_Biological_Process_2023",
        "top_n": 5
      },
      {
        "gene_list": [
          "SOD1",
          "SOD2",
          "CAT",
          "GPX1",
          "PRDX1"
        ],
        "library": "KEGG_2021_Human",
        "top_n": 5
      }
    ],
    "fields": {
      "operation": "enrich"
    }
  },
  {
    "name": "Enrichr_get_top_enriched",
    "description": "Get top enriched terms across multiple libraries simultaneously. Submit a gene list and get the most significant pathways, processes, and functions from GO, KEGG, Reactome, and WikiPathways in one call. Ideal for quick functional annotation overview. Set species to query the fly, worm, yeast, or fish Enrichr instances; libraries and gene symbols differ per organism.",
    "type": "EnrichrExtTool",
    "parameter": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "const": "get_top_enriched",
          "description": "Operation type (fixed: get_top_enriched)",
          "default": "get_top_enriched"
        },
        "gene_list": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of gene symbols (e.g., ['TP53', 'BRCA1', 'EGFR'])"
        },
        "libraries": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Libraries to query (default: GO_Biological_Process_2023, KEGG_2021_Human, Reactome_2022, WikiPathways_2024_Human)"
        },
        "top_n": {
          "type": "integer",
          "description": "Number of top terms per library (default: 5)",
          "default": 5
        },
        "species": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            null,
            "human",
            "mouse",
            "fly",
            "worm",
            "yeast",
            "fish"
          ],
          "description": "Organism instance to query: 'human' (default), 'mouse', 'fly' (FlyEnrichr), 'worm' (WormEnrichr), 'yeast' (YeastEnrichr), or 'fish' (FishEnrichr). Gene set libraries differ per organism, and gene symbols must match that organism's nomenclature."
        }
      },
      "required": [
        "gene_list"
      ]
    },
    "return_schema": {
      "type": "object"
    },
    "test_examples": [
      {
        "gene_list": [
          "TP53",
          "BRCA1",
          "EGFR",
          "KRAS",
          "MYC",
          "BRAF",
          "PTEN",
          "RB1",
          "APC",
          "VHL"
        ],
        "top_n": 3
      }
    ],
    "fields": {
      "operation": "get_top_enriched"
    }
  },
  {
    "name": "Enrichr_gene_to_genesets",
    "description": "Reverse gene-to-geneset membership lookup. Given ONE gene symbol, return every Enrichr term/gene-set (by name) that contains it, broken down across all ~243 Enrichr libraries (KEGG/Reactome/WikiPathways pathways, GO terms, transcription-factor target sets, disease/drug signatures, GeneSigDB publication tables, etc.). This is the inverse of forward enrichment (which takes a gene list): use it to ask 'what known programs/signatures is gene X a member of?' without running an enrichment. Returns a dict of library_name -> list of term/gene-set names containing the gene. Set include_metadata=true to also return per-library category/description metadata. No authentication required. Set species to query the fly, worm, yeast, or fish Enrichr instances; libraries and gene symbols differ per organism.",
    "type": "EnrichrExtTool",
    "parameter": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "const": "gene_to_genesets",
          "description": "Operation type (fixed: gene_to_genesets)",
          "default": "gene_to_genesets"
        },
        "gene": {
          "type": "string",
          "description": "A single official HGNC gene symbol (e.g., 'STAT3', 'BRCA1'). Case-sensitive; use the canonical symbol."
        },
        "include_metadata": {
          "type": "boolean",
          "description": "If true, also return library category/description metadata (Enrichr setup=true). Default: false.",
          "default": false
        },
        "max_terms_per_library": {
          "type": "integer",
          "description": "Optional cap on the number of term names returned per library (0 = no cap, return all). Default: 0.",
          "default": 0
        },
        "species": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            null,
            "human",
            "mouse",
            "fly",
            "worm",
            "yeast",
            "fish"
          ],
          "description": "Organism instance to query: 'human' (default), 'mouse', 'fly' (FlyEnrichr), 'worm' (WormEnrichr), 'yeast' (YeastEnrichr), or 'fish' (FishEnrichr). Gene set libraries differ per organism, and gene symbols must match that organism's nomenclature."
        }
      },
      "required": [
        "gene"
      ]
    },
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "gene": {
              "type": "string",
              "description": "The queried gene symbol"
            },
            "num_libraries": {
              "type": "integer",
              "description": "Number of Enrichr libraries in which the gene appears in at least one term"
            },
            "total_gene_sets": {
              "type": "integer",
              "description": "Total number of term/gene-set memberships across all libraries"
            },
            "libraries": {
              "type": "object",
              "description": "Map of library_name -> list of term/gene-set names in that library containing the gene"
            },
            "descriptions": {
              "type": "array",
              "description": "Per-library description metadata (only when include_metadata=true)"
            },
            "categories": {
              "description": "Library category metadata (only when include_metadata=true)"
            }
          },
          "required": [
            "gene",
            "num_libraries",
            "total_gene_sets",
            "libraries"
          ]
        },
        {
          "type": "object",
          "properties": {
            "error": {
              "type": "string"
            }
          },
          "required": [
            "error"
          ]
        }
      ]
    },
    "test_examples": [
      {
        "gene": "STAT3"
      },
      {
        "gene": "BRCA1",
        "max_terms_per_library": 5
      }
    ],
    "fields": {
      "operation": "gene_to_genesets"
    }
  }
]
