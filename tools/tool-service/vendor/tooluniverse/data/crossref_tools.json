[
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_search_works",
    "description": "Search Crossref Works API for scholarly articles, publications, and research outputs by keyword. Returns metadata including title, abstract, journal, authors, publication year, DOI, URL, citations, and publisher. Supports filtering by publication type (journal article, conference paper, etc.) and date range. Use this to discover academic literature, verify citations, or build bibliographies. Find DOIs to use with Crossref_get_work for complete metadata. The response reports 'count' (works in this page) alongside 'total_count' (works matching the query in all of Crossref) -- these differ on almost every real search, since 'limit' defaults to 10 and caps at 100, so treat 'count' as a page size and never as the size of the literature. When they differ, 'truncated' is true and 'truncation_note' gives the 'offset' for the next page. Note: Crossref's own API weakens keyword relevance-matching whenever a non-default 'sort' query param is combined with 'query' (e.g. sort=is-referenced-by-count), returning globally top-cited works largely unrelated to the search term -- omit 'sort' (the default relevance ranking) unless you have separately verified the combination for your query.",
    "parameter": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query for Crossref works. Use keywords separated by spaces to refine your search across titles, abstracts, authors, and other bibliographic fields."
        },
        "limit": {
          "type": "integer",
          "description": "Number of articles to return in this page. This sets the maximum number of articles retrieved from Crossref. Max 100 per request. It does not change 'total_count', which always reports how many works match the query.",
          "default": 10,
          "minimum": 1,
          "maximum": 100
        },
        "offset": {
          "type": "integer",
          "minimum": 0,
          "description": "Skip this many matching works before returning results. Use with 'limit' to page through a result set larger than 100; the response 'truncation_note' gives the offset for the next page."
        },
        "filter": {
          "type": "string",
          "description": "Optional filter string for Crossref API. Examples: 'type:journal-article' (only journal articles), 'from-pub-date:2020-01-01' (published after date), 'has-abstract:true' (only with abstracts). Multiple filters comma-separated."
        }
      },
      "required": [
        "query"
      ]
    },
    "return_schema": {
      "type": "array",
      "description": "Array of Crossref work objects (articles/publications)",
      "items": {
        "type": "object",
        "properties": {
          "DOI": {
            "type": "string"
          },
          "title": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "author": {
            "type": "array"
          },
          "container-title": {
            "type": "array",
            "description": "Journal name"
          },
          "published": {
            "type": "object"
          },
          "publisher": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "Publication type (e.g., journal-article)"
          },
          "is-referenced-by-count": {
            "type": "integer",
            "description": "Citation count"
          },
          "abstract": {
            "type": "string"
          },
          "reference": {
            "type": "array",
            "description": "List of references"
          }
        }
      }
    },
    "test_examples": [
      {
        "query": "artificial intelligence",
        "limit": 2
      },
      {
        "query": "machine learning",
        "limit": 1,
        "filter": "type:journal-article,from-pub-date:2020-01-01"
      }
    ],
    "fields": {
      "endpoint": "https://api.crossref.org/works",
      "method": "GET"
    }
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_get_work",
    "description": "Get complete metadata for a specific scholarly work by its DOI (Digital Object Identifier). Returns comprehensive bibliographic information including title, abstract, full author list with affiliations and ORCIDs, journal details, publication dates, volume/issue/pages, references, citations, license, funding information, and related identifiers. Essential for verifying citations, extracting complete metadata, or building reference managers. DOIs can be found using Crossref_search_works (format: 10.####/...).",
    "parameter": {
      "type": "object",
      "properties": {
        "doi": {
          "type": "string",
          "description": "Digital Object Identifier in format '10.####/...' (e.g., '10.1038/nature12373', '10.1371/journal.pone.0123456'). Find DOIs using Crossref_search_works or from publication citations."
        }
      },
      "required": [
        "doi"
      ]
    },
    "fields": {
      "endpoint": "https://api.crossref.org/works/{doi}",
      "method": "GET"
    },
    "return_schema": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "properties": {
        "DOI": {
          "type": "string"
        },
        "title": {
          "type": "array"
        },
        "abstract": {
          "type": "string"
        },
        "author": {
          "type": "array"
        },
        "container-title": {
          "type": "array"
        },
        "published": {
          "type": "object"
        },
        "publisher": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "is-referenced-by-count": {
          "type": "integer"
        },
        "reference": {
          "type": "array"
        },
        "license": {
          "type": "array"
        },
        "funder": {
          "type": "array"
        }
      }
    },
    "test_examples": [
      {
        "doi": "10.1038/nature12373"
      },
      {
        "doi": "10.1371/journal.pone.0123456"
      }
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_get_journal",
    "description": "Get metadata for a specific journal by its ISSN (International Standard Serial Number). Returns journal details including title, publisher, ISSN variants (print/electronic), coverage dates, and publication frequency. Use this to verify journal information, assess journal credibility for submissions, check publisher details, or validate ISSNs. Find ISSNs from publication metadata or journal databases (format: ####-####).",
    "parameter": {
      "type": "object",
      "properties": {
        "issn": {
          "type": "string",
          "description": "Journal ISSN in format ####-#### (e.g., '1476-4687' for Nature, '1932-6203' for PLOS ONE). Both print and electronic ISSNs are accepted."
        }
      },
      "required": [
        "issn"
      ]
    },
    "fields": {
      "endpoint": "https://api.crossref.org/journals/{issn}",
      "method": "GET"
    },
    "return_schema": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "properties": {
        "title": {
          "type": "string"
        },
        "publisher": {
          "type": "string"
        },
        "ISSN": {
          "type": "array"
        },
        "coverage": {
          "type": "object"
        },
        "flags": {
          "type": "object"
        }
      }
    },
    "test_examples": [
      {
        "issn": "1476-4687"
      },
      {
        "issn": "1932-6203"
      }
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_list_funders",
    "description": "Search and list research funding organizations in the Crossref database. Returns funder details including funder ID, name, location, DOI count, and alternative names. Use this to discover funder identifiers for filtering publications by funding source, or to explore research funding patterns. Essential for grant analysis, funding compliance, or finding publications funded by specific organizations (e.g., NIH, NSF, ERC). The response reports 'count' (funders in this page) alongside 'total_count' (funders matching the query in all of Crossref); 'limit' defaults to 20, so treat 'count' as a page size. When they differ, 'truncated' is true and 'truncation_note' gives the 'offset' for the next page.",
    "parameter": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query for funder names (e.g., 'National Science Foundation', 'NIH', 'European Research Council', 'Wellcome Trust')."
        },
        "limit": {
          "type": "integer",
          "description": "Maximum number of funders to return in this page (default: 20, max: 100). It does not change 'total_count', which always reports how many funders match.",
          "default": 20,
          "minimum": 1,
          "maximum": 100
        },
        "offset": {
          "type": "integer",
          "minimum": 0,
          "description": "Skip this many matching funders before returning results. Use with 'limit' to page through the full result set; the response 'truncation_note' gives the offset for the next page."
        }
      }
    },
    "fields": {
      "endpoint": "https://api.crossref.org/funders",
      "method": "GET"
    },
    "return_schema": {
      "type": [
        "array",
        "null"
      ],
      "description": "List of funders.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "alt-names": {
            "type": "array"
          },
          "tokens": {
            "type": "array"
          }
        }
      }
    },
    "test_examples": [
      {
        "query": "National Science Foundation",
        "limit": 5
      },
      {
        "query": "NIH"
      }
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_get_funder",
    "description": "Get detailed metadata for a specific research funder by its Crossref funder ID. Returns comprehensive funder information including official name, alternative names, location, hierarchy (parent organizations), DOI count, and URI. Use this to understand funder details, verify funder identifiers, or explore funding relationships. Find funder IDs using Crossref_list_funders (format: numeric ID like '100000001' for NSF, '100000002' for NIH).",
    "parameter": {
      "type": "object",
      "properties": {
        "funder_id": {
          "type": "string",
          "description": "Crossref funder identifier (e.g., '100000001' for NSF, '100000002' for NIH, '501100000780' for European Commission). Find IDs using Crossref_list_funders."
        }
      },
      "required": [
        "funder_id"
      ]
    },
    "fields": {
      "endpoint": "https://api.crossref.org/funders/{funder_id}",
      "method": "GET"
    },
    "return_schema": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true,
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "location": {
          "type": "string"
        },
        "alt-names": {
          "type": "array"
        },
        "uri": {
          "type": "string"
        },
        "hierarchy": {
          "type": "object"
        }
      }
    },
    "test_examples": [
      {
        "funder_id": "100000001"
      },
      {
        "funder_id": "100000002"
      }
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_list_types",
    "description": "List all publication types available in Crossref (e.g., journal-article, book-chapter, conference-paper, dataset, preprint). Returns type identifiers and labels. Use this to discover valid publication type filters for Crossref_search_works, understand the classification of research outputs, or analyze publication type distributions. Essential for constructing precise search queries or filtering results by publication category.",
    "parameter": {
      "type": "object",
      "properties": {}
    },
    "fields": {
      "endpoint": "https://api.crossref.org/types",
      "method": "GET"
    },
    "return_schema": {
      "type": [
        "array",
        "null"
      ],
      "description": "List of publication types.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "id": {
            "type": "string"
          },
          "label": {
            "type": "string"
          }
        }
      }
    },
    "test_examples": [
      {}
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_search_members",
    "description": "Search Crossref members (publishers / depositing organizations) by name. Returns each member's numeric Crossref member id, primary name, location, and DOI-coverage analytics: counts of current, backfile, and total DOIs, plus per-year DOI breakdowns and prefixes. Use this to find a publisher's member id (for Crossref_get_member), compare publisher output, or analyze DOI/journal coverage. Example: query 'plos' -> member id 340 'Public Library of Science (PLoS)'. The response reports 'count' (members in this page) alongside 'total_count' (members matching the query in all of Crossref) -- e.g. query 'university' matches 4032, far more than the default 'limit' of 20 returns -- so treat 'count' as a page size. When they differ, 'truncated' is true and 'truncation_note' gives the 'offset' for the next page.",
    "parameter": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Publisher / member name to search (e.g., 'plos', 'elsevier', 'springer', 'wiley'). Prefer a single distinctive word: Crossref's own search can return zero results for multi-word brand names (e.g. 'springer nature' finds nothing even though 'springer' alone matches)."
        },
        "limit": {
          "type": "integer",
          "description": "Maximum number of members to return in this page. Max 100 per request. It does not change 'total_count', which always reports how many members match.",
          "default": 20,
          "minimum": 1,
          "maximum": 100
        },
        "offset": {
          "type": "integer",
          "minimum": 0,
          "description": "Skip this many matching members before returning results. Use with 'limit' to page through the full result set; the response 'truncation_note' gives the offset for the next page."
        }
      },
      "required": [
        "query"
      ]
    },
    "fields": {
      "endpoint": "https://api.crossref.org/members",
      "method": "GET"
    },
    "return_schema": {
      "type": "array",
      "description": "List of Crossref member (publisher) records matching the search query, each with DOI-coverage analytics.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "id": {
            "type": "integer"
          },
          "primary-name": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "counts": {
            "type": "object",
            "description": "current-dois, backfile-dois, total-dois"
          },
          "breakdowns": {
            "type": "object",
            "description": "dois-by-issued-year"
          },
          "prefixes": {
            "type": "array"
          },
          "flags": {
            "type": "object"
          },
          "coverage": {
            "type": "object"
          }
        }
      }
    },
    "test_examples": [
      {
        "query": "plos",
        "limit": 2
      },
      {
        "query": "elsevier",
        "limit": 2
      }
    ]
  },
  {
    "type": "CrossrefRESTTool",
    "name": "Crossref_get_member",
    "description": "Get a specific Crossref member (publisher) by its numeric member id, with full DOI-coverage analytics: primary name, location, counts of current/backfile/total DOIs, per-year DOI breakdowns (dois-by-issued-year), DOI prefixes, and deposit/coverage flags. Use this to audit a publisher's DOI output over time or verify member metadata. Find ids with Crossref_search_members. Example: member id 78 -> 'Elsevier BV', total-dois 24961410.",
    "parameter": {
      "type": "object",
      "properties": {
        "member_id": {
          "type": "string",
          "description": "Numeric Crossref member id (e.g., '78' for Elsevier BV, '340' for PLOS). Find ids using Crossref_search_members."
        }
      },
      "required": [
        "member_id"
      ]
    },
    "fields": {
      "endpoint": "https://api.crossref.org/members/{member_id}",
      "method": "GET"
    },
    "return_schema": {
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": true,
          "description": "Crossref member (publisher) metadata with DOI-coverage analytics.",
          "properties": {
            "id": {
              "type": "integer"
            },
            "primary-name": {
              "type": "string"
            },
            "location": {
              "type": "string"
            },
            "counts": {
              "type": "object",
              "description": "current-dois, backfile-dois, total-dois"
            },
            "breakdowns": {
              "type": "object",
              "description": "dois-by-issued-year"
            },
            "prefixes": {
              "type": "array"
            },
            "flags": {
              "type": "object"
            },
            "coverage": {
              "type": "object"
            }
          }
        },
        {
          "type": "object",
          "required": [
            "status",
            "error"
          ],
          "properties": {
            "status": {
              "const": "error"
            },
            "error": {
              "type": "string"
            }
          }
        }
      ]
    },
    "test_examples": [
      {
        "member_id": "78"
      },
      {
        "member_id": "340"
      }
    ]
  }
]
