# Python side
scanpy>=1.10
anndata
numpy
# R side (install separately on the server):
#   R >= 4.2 with Bioconductor package: slingshot (+ Matrix, jsonlite)
#   Rscript must be on PATH (or set RSCRIPT_BIN).
#   BiocManager::install("slingshot")
