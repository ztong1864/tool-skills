interface:
  display_name: "LabKAG API"
  short_description: "API wrapper for calling LabKAG through the bundled CLI script."
  default_prompt: "Use $labkag-skill to call the LabKAG API through the bundled CLI script."

policy:
  allow_implicit_invocation: true
