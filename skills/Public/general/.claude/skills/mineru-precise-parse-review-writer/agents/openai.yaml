interface:
  display_name: "MinerU Precise Parse"
  short_description: "Batch-parse local PDFs with MinerU"
  default_prompt: "Use $mineru-precise-parse-review-writer to batch-convert local review PDFs into Markdown with MinerU precise parsing."
policy:
  allow_implicit_invocation: true
