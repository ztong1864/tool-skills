interface:
  display_name: "Review Export DOCX"
  short_description: "Convert review Markdown to ACS-style DOCX."
  default_prompt: "Use $review-export-docx to convert the finalized review Markdown into a Word DOCX with the bundled ACS-style template. When a validated review_summary_chart.json exists beside the draft, require its current full-review and per-section PNG bundle and embed those charts automatically."
policy:
  allow_implicit_invocation: true
