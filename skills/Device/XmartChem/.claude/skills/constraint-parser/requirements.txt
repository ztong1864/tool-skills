# constraint-parser dependencies
# Used by scripts/parse_constraints.py

# Table conversion (.xlsx/.xls/.csv -> CSV)
pandas>=1.5.0

# Calls the OpenAI-compatible LLM endpoint (OPENAI_BASE_URL) to parse instructions into JSON
requests>=2.28.0

# Loads OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL from .env
python-dotenv>=1.0.0
